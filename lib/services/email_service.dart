/// EduX School Management System
/// Email Service - Stubbed (Feature Removed)
library;

/// Service to handle email operations (Stubbed)
class EmailService {
  EmailService();

  /// Send an email (Stubbed - always returns false)
  Future<bool> sendEmail({
    required String to,
    required String subject,
    required String body,
    bool isHtml = false,
  }) async {
    // Feature removed
    return false;
  }
}
